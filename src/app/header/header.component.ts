import { Component, EventEmitter, Output } from '@angular/core';

@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.css']
})
export class HeaderComponent {
/*
  @Output() onHeaderClickEvent = new EventEmitter<string>;

  onNavItemClicked(navSelectedItem: string){

    console.log(navSelectedItem,"got clicked and is emitted to app component ")

    this.onHeaderClickEvent.emit(navSelectedItem);

  }
*/

}
